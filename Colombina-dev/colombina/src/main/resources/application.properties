spring.application.name=colombina

# Server
server.port=8080

# DB
spring.datasource.url=jdbc:postgresql://javeriana.cluster-cu5yf7r7w7fs.us-east-1.rds.amazonaws.com:5432/regulatorios
spring.datasource.username=postgres
spring.datasource.password=BamXZ2ePzpGXD0jwiUGQ
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.generate-ddl=true
spring.jpa.hibernate.ddl-auto=create

# Files
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Logging
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Environment
spring.config.import: file:../.env[.properties]

# JWT validation
jwt.signing.key=${JWT_SECRET}

# Emails
resend.api.key=${RESEND_API_KEY}

# Minio
minio.access.key=${MINIO_ACCESS_KEY}
minio.access.secret=${MINIO_SECRET_KEY}
minio.url=http://localhost:${MINIO_PORT}

# AWS
aws.access.key=${AWS_ACCESS_KEY}
aws.secret.key=${AWS_SECRET_KEY}
